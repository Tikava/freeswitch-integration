import { AsyncTransform, getCallbackWriteStream } from "./lib";

AsyncTransform.from([1, 2, 3, 4, 5]).pipe(
  getCallbackWriteStream((item, resolve) => {
    console.log(item);
    resolve();
  }),
  { await: false }
);
