import { AsyncReadStream } from "@mojsoski/async-stream";
import { AudioFormat } from "@silyze/async-audio-stream";
export default class ULawFormat extends AudioFormat {
    #private;
    constructor(sampleRate: number);
    get name(): string;
    get pcmSampleRate(): number;
    encode(input: AsyncReadStream<Buffer>): import("@mojsoski/async-stream").AsyncTransform<Buffer<ArrayBufferLike>>;
    decode(input: AsyncReadStream<Buffer>): import("@mojsoski/async-stream").AsyncTransform<Buffer<ArrayBufferLike>>;
}
